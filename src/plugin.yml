name: ${project.name}
author: ${authorName}
version: ${project.version}
main: ${project.groupId}.${main}
depend: [ProjectKorra]
api-version: 1.16
permissions:
  bending.command.hyperion:
    default: true
    description: Shows info about Hyperion.
  bending.command.hyperion.reload:
    default: op
    description: Reload the config of Hyperion.
